"""
# format the output to data type int by using int()
# this way you put the output of input as a argument into the int() function and assign it to a variable
"""
# x = int(input("What is x?: "))
# y = int(input("What is y?: "))

# z = x + y

print(f"{x} + {y} = {z}")

"""
Keep your code as simple as possible,
don't create a variable if you don't need it

prevent creating the z variable by using the + operator inside the print function
"""

# print(f"{x} + {y} = {x + y}")

# You can even do this:
# print(int(input("What is x?: ")) + int(input("What is y?: ")))

# be careful and don't nest too many things inside each other for clearity and readability